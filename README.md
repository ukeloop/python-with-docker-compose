# python with docker-compose
Use python with Docker Compose.

## Usage

Setup
```bash
$ docker-compose up -d
```

Enter the container
```bash
$ docker-compose exec python bash
```

Remove
```bash
$ docker-compose down
```